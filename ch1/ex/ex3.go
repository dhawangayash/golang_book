package main

import (
    "fmt"
    "os"
)

/*
    1.3 Experiment the measure the difference in running time between 
        our potentially inefficient versions and the one that uses 
        `strings.Join`.

*/
func main() {
    
}
